import { Typography } from '@mui/material';

const NotFound = () => (
  <Typography>
    Oops looks like you have a bad code; try again with a working code.
  </Typography>
);

export default NotFound;
